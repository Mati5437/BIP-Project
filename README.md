
  # Component breakdown with login

  This is a code bundle for Component breakdown with login. The original project is available at https://www.figma.com/design/E8W12t51KFpu4JEoW7JqZ1/Component-breakdown-with-login.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  